//@version=5
strategy("OutSide Bar Strategy", overlay=true)
if (high > high[1] and low < low[1])
	if (close > open)
		strategy.entry("OutBarLE", strategy.long, comment="OutBarLE")
	if (close < open)
		strategy.entry("OutBarSE", strategy.short, comment